name: apprain_barcode_scanner_platform_interface
description: A common platform interface for the apprain_barcode_scanner plugin.
version: 1.0.0
repository: https://github.com/yagmure15/apprain_barcode_scanner

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^6.0.0
